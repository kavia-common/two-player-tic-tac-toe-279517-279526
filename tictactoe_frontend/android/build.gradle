// Stub build.gradle for CI environment. Not for real builds.
buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.7.0")
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

task("assembleDebug") {
    doLast {
        println("Stub assembleDebug task executed.")
    }
}
