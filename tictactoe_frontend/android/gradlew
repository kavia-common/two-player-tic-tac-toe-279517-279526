#!/usr/bin/env bash
# Stub gradlew for CI environments without Android tooling.
# Exits successfully to avoid failing lint/analysis pipelines that call ./gradlew.
echo "Stub gradlew: Android build disabled in this environment."
exit 0
